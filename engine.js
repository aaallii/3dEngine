function loadScreen(){
  const camera = { x: 50, y: 50, z: 50 };
  fetch("nodes.json")
  .then(response=>response.json)
  .then(data=>{
    for(let i=1,data[String(i)],i++){
      let pageData=data[String(i)]
      let x=0
      let z=0
      let y=0
    if(Array.isArray(pageData){
        
      }}})
}
